type: object
discriminator: amountPolicy
properties:
  amountPolicy:
    type: string
    enum:
      - "full-price"
      - "partial-amount"
      - "rate-discount"
      # TODO: Unsupported at the moment
      # - "fixed-amount"
      # - "fixed-discount"
      # - "rate-penalty"
      # - "fixed-penalty"
required:
  - amountPolicy
