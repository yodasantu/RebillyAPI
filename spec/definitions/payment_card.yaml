description: "A Payment using a Payment Card"
allOf:
  -
    $ref: "#/definitions/Payment"
  -
    type: "object"
    properties:
       paymentInstrument:
          $ref: "#/definitions/PaymentInstrument"
    required:
      - "paymentInstrument"
