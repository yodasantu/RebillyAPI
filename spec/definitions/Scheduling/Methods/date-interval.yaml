allOf:
  - $ref: "#/definitions/ScheduleInstruction"
  - type: object
    properties:
      duration:
        type: integer
        minimum: 1
      unit:
        $ref: "#/definitions/TimeUnit"
    required:
      - duration
      - unit
