description: minimum-order-amount restrictions
allOf:
  -
    $ref: "#/definitions/CouponRestriction"
  -
    type: "object"
    required:
     - "amount"
     - "currency"
    properties:
      currency:
        type: string
        description: Restriction currency
      amount:
        type: string
        description: Restriction amount
