type: object
description: In condition
allOf:
  - $ref: "#/definitions/Condition"
  - type: object
    properties:
      field:
        type: string
      values:
        type: array
        items:
          type: string
    required:
      - field
      - values
