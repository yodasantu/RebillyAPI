type: object
description: Between condition
allOf:
  - $ref: "#/definitions/Condition"
  - type: object
    properties:
      field:
        type: string
      min:
        type: string
      max:
        type: string
    required:
      - field
      - min
      - max
