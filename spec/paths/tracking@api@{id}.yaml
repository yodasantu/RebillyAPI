parameters:
  - $ref: "#/parameters/resourceId"
get:
  tags:
    - Tracking
  summary: Retrieve a tracking API log with specified identifier string
  responses:
    200:
      description: Tracking API log was retrieved successfully
      headers:
        X-Rate-Limit-Limit:
          $ref: "#/headers/X-Rate-Limit-Limit"
        X-Rate-Limit-Remaining:
          $ref: "#/headers/X-Rate-Limit-Remaining"
        X-Rate-Limit-Reset:
          $ref: "#/headers/X-Rate-Limit-Reset"
      schema:
        $ref: "#/definitions/Tracking/ApiTracking"
    404:
      $ref: "#/responses/NotFound"
    401:
      $ref: "#/responses/AccessForbidden"
